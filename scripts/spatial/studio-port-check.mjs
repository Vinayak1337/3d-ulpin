import {chromium} from '@playwright/test';
import {mkdir,writeFile} from 'node:fs/promises';
import assert from 'node:assert/strict';
const out='docs/evidence/t058/port';await mkdir(out,{recursive:true});
const browser=await chromium.launch({channel:'chrome',headless:true});
const report={errors:[],checks:[],source:'Copied City Studio inside 3d-ulpin',testedAt:new Date().toISOString()};
try{
 const page=await browser.newPage({viewport:{width:1672,height:941},deviceScaleFactor:1});page.setDefaultTimeout(30000);
 page.on('pageerror',e=>report.errors.push(e.message));
 await page.goto('http://127.0.0.1:3000/studio',{waitUntil:'domcontentloaded',timeout:120000});
 await page.waitForFunction(()=>window.__CITY_DEBUG__?.controlConnected,{},{timeout:120000});await page.waitForTimeout(4000);
 await page.screenshot({path:out+'/map.png'});assert.equal(await page.locator('.app-header').count(),1);
 assert.equal(await page.getByTestId('selected-ulpin').textContent(),'11007500003527');
 await page.locator('.map-viewport canvas').evaluate(c=>c.dataset.studioIdentity='original');
 report.checks.push('Copied Studio composition with one header and original default property');
 await page.getByRole('button',{name:'Utilities',exact:true}).click();assert.match(page.url(),/tab=utilities/);
 await page.goBack();assert.equal(await page.locator('.ins-tabs button.active').textContent(),'Overview');
 assert.equal(await page.locator('.map-viewport canvas').getAttribute('data-studio-identity'),'original');
 report.checks.push('Inspector routing and browser Back preserve the canvas');
 await page.getByRole('button',{name:'Open register',exact:true}).click();await page.waitForSelector('.register-overview');await page.waitForTimeout(1800);
 assert.match(page.url(),/\/studio\/register\//);await page.screenshot({path:out+'/register.png'});
 await page.locator('[data-unit-id="BLD-0413/F1/U1"]').click();assert.match(page.url(),/unit=BLD-0413%2FF1%2FU1/);
 await page.getByRole('button',{name:'Inspect record',exact:true}).click();assert.match(page.url(),/doc=lease/);
 await page.reload();await page.waitForSelector('.rec-document',{timeout:120000});
 assert.equal(await page.getByLabel('Unit and occupant',{exact:true}).inputValue(),'BLD-0413/F1/U1');
 report.checks.push('Exact unit/lease deep link survives full reload');
 await page.goto('http://127.0.0.1:3000/studio/map/not-a-property');await page.getByRole('heading',{name:'That record is unavailable'}).waitFor();assert.equal(await page.locator('.map-viewport canvas').count(),0);
 report.checks.push('Unknown property does not silently display the showcase building');
 assert.deepEqual(report.errors,[]);report.result='PASS';
}catch(error){report.result='FAIL';report.error=String(error);console.error(error);process.exitCode=1;}finally{await browser.close();await writeFile(out+'/results.json',JSON.stringify(report,null,2));console.log(JSON.stringify(report));}
