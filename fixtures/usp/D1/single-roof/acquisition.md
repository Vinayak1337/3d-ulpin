# D1 single-roof source receipt

Retrieved on 2026-09-23 from the [3DBAG single-building API](https://api.3dbag.nl/collections/pand/items/NL.IMBAG.Pand.1655100000500568) with `Accept: application/json`. The command used `curl --fail --location --silent --show-error --max-time 45 --retry 2 --retry-delay 2 --retry-max-time 110 --dump-header response.headers --output original.json`; exit 0, HTTP 200, `content-type: application/json`, 6,783 response bytes. The response headers report `content-crs: <http://www.opengis.net/def/crs/EPSG/0/7415>` and `date: Wed, 23 Sep 2026 15:21:30 GMT`. No retry was needed. `original.json` is the exact response body; `response.headers` is the exact returned header block. The server supplied no `ETag`, `Last-Modified`, or source release number. The object has a `b3_pw_datum` value of 2024; this is an attribute of the building and is not a release version for the response.

Provider [webservice documentation](https://docs.3dbag.nl/en/delivery/webservices/) says the API returns CityJSONFeature objects in Amersfoort / RD New + NAP height, EPSG:7415. Its 3D Tiles use EPSG:4978 and glTF extensions, a separate delivery path. The [copyright page](https://docs.3dbag.nl/en/copyright/) applies CC BY 4.0 to webservice data, including redistribution and adaptation with credit, licence link, and a change indication. Required credit: “© 3DBAG by tudelft3d and 3DGI”; digital maps also need a link to that copyright page, at bottom right. This copy is unchanged. These provider declarations establish catalogue and permission evidence; no coordinate operation to an ellipsoidal/global frame was performed.

The independent oracle in `expected.json` came from direct inspection of the preserved JSON with Python's standard `json` module, without using a product parser or display adapter. For each integer vertex `v`, the only decode applied was `v[i] × metadata.transform.scale[i] + metadata.transform.translate[i]`. All 62 decoded vertices determine the expected EPSG:7415 bounds. Building and part IDs, parent/child links, LoD, face topology and semantic surface indices were read from `feature.CityObjects`; each Solid's `semantics.values[0]` was paired by position with `boundaries[0]`. A roof face was counted nonhorizontal when its decoded Z span exceeded 0.000001 m. The LoD 2.2 three roof rings have 4, 10 and 6 vertices. Their plane deviations are below 0.001 m when a maximal-area vertex triplet establishes each plane; using the first three vertices alone is unstable on nearly collinear ring segments. There are no hole rings in this particular response, though an adapter must not generally assume this. The LoD 2.2 part bounds are X 91447.4215–91464.0575, Y 398435.80225–398452.41925, Z 0.9195041809082016–8.514504180908202 m. The all-vertex minimum Z is the separate LoD 0 footprint at 0.0005041809082015902 m.

The building's `b3_bouwlagen` is null. The source provides no internal floor geometry, unit boundaries or rights. The LoD 2.2 slopes and the provider's roof attributes describe an external reconstruction; they cannot establish apartment records. The displayed sample can use a labelled local engineering frame anchored near the decoded bounds; global placement and measurement joins need a separately qualified transformation from EPSG:7415/NAP. The D1 product parsing, roof triangulation, selection, rendering and record workflow stages have not run.
