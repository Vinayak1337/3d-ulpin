# Supplied V2 design pack

These documents and the image manifest are copied from `3D_ULPIN_V2_REDESIGN_PACK_FINAL`, supplied by the user. The build prompt is the implementation brief; project safety and canonical data contracts remain authoritative. The user's later explicit route-renaming request is implemented with `/legacy` and compatibility redirects.

All 35 original reference images were inspected locally. They are not dependencies or live backgrounds. Final application captures are separately recorded under `docs/evidence/v2`.
