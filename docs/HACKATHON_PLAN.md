# Today's 4–8 hour delivery

Start from preserved source documents, with the user's latest deadline and core-demo scope taking precedence. Preserve the full core stack. Focus on upload → inspection → prepared candidates → computed and saved 3D model → evidence-linked findings → correction.

1. First 60–90 minutes: establish shared contracts, boot infrastructure, and independently build processors and the viewer shell.
2. Next 90 minutes: integrate the first actual uploaded C001 model end-to-end.
3. Next 90 minutes: source previews, linked plan/3D selection, geometry edit, revised levels, and computed conflict correction.
4. Remaining time: PDF/tracing/floor controls, second-case tests, production build, startup/restart checks, and three rehearsals. Reserve final quarter for verification; do not spend it on optional AI or cosmetic expansion.

Lead owns contracts, Next application API, persistence, job integration, and acceptance. Platform worker owns runtime. Geometry worker owns inspectors, computational geometry, and fixtures. Web worker owns frontend. Work overlaps only across frozen interfaces.

Today omits multi-user identity, Android, formal review/acceptance, national ID issuance, offline sync, arbitrary solids, AI extraction, IFC/point clouds, and public deployment. Optional Nous API is allowed only to solve a concrete blocking text-input problem with verified free access; core inputs and math do not need it.

The release label is HACKATHON_DEMO_VERIFIED only after real input-to-model processing, persistence, C001 correction, independent second case, and repeatable browser rehearsals pass. Partial results state exact gaps.
