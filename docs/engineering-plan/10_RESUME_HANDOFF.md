# Standalone continuation brief

Continue Vinayak's `3d-ulpin` work on `feat/unified-spatial-foundation`. The branch inherits the real reusable-map implementation at `f082441`; do not rebuild that work blindly. `backlog.json` is the live status authority: read its next task and latest accepted result, then verify actual Git state. T001 through T005 are accepted as of the T005 source-contract checkpoint `39a507d`; T006 has a detailed plan. PC command/file access works. Local regression/build and isolated hosted checks remain distinct; no private-PC source data or database has been reset.

Read the master plan, guidelines, `backlog.json` and the last completed task result. Only one implementation task is active. Do not reapply the original adoption patch, recreate the branch, delete new test reports or rerun old proof cleanup. Future detailed-plan paths remain reserved until actually authored. Historical progress notes are not the current status ledger.

The historical repository was `E:\Projects\3d-ulpin` on main at `16220977b127dbdad8953c899a38f37e8c6a53d6` with `ulpin-canonical/2`. Recheck actual Git status/HEAD/worktrees, instructions and concurrent changes. Preserve existing code and data. Start or safely reuse `feat/unified-spatial-foundation`; never reset/stash others' work or mutate populated Docker volumes as a setup shortcut.

The first task is baseline and isolation, not schema or UI coding. Discover actual test commands from current package/configuration files; inspect mutation effects and isolate database/object resources before running them. Record protected IDs, original hashes, historical frame metadata and current results. Do not call remote or physical-device checks passed when only local/headless evidence exists.

Then advance through the bounded task sequence: thin contracts → legacy adapters/normalization → persisted two-block fixture → shared query/session/viewport → compiler/real tiles/measurement qualification → full-neighbourhood fidelity → cross-area resources/recovery → map UI → register/floor/unit/evidence → import/edit/review → integrated release. Future BIM/cloud/survey/private integrations and extreme scale are elective increments, not prerequisites for a useful first release.

Use the existing product core and its Next.js/React, PostGIS/object storage and private geometry/worker boundaries. Cesium is the first renderer candidate, not an unconditional guarantee; qualify the pinned implementation before changing engines. City Studio is a separate synthetic prototype, not the authoritative geography or registry. Reuse useful lessons without importing a second data truth.

The shared contract must retain separate original assets, observations, selected canonical representations and disposable render assets. Keep stable entity/source/external/tile IDs distinct. Preserve source/analytical/world/render frames and vertical meaning. Unknown height/depth stays unknown; exterior models do not prove floors or residents. Documents are optional when sufficient geometry exists. Model, plan, table, findings and exports resolve the same identities/revisions/quantity definitions. Public/geometry-safe output excludes private parties and source URLs.

The user wants mockup-level visual quality in an actual interactive complete neighbourhood. A single hero building, static background or green build is not acceptance. Use common recipes across dense, sloped, sparse and held-out fixtures; do not widen Uttam Nagar lanes or move buildings to beautify them. User visual acceptance and numerical/identity/resource gates are separate and all must pass.

Before each task, write its own detailed plan using the template: current files, outcome/exclusions, contracts/state, dependencies, protected invariants, alternatives, failure/concurrency/migration/security cases, independent test oracles, step sequence, rollback and Definition of Done. Split oversized tasks into children preserving references. Do not ask for approval of every routine technical action already in scope; do stop for unresolved consequential choices, destructive ambiguity, required visual acceptance or missing critical environment access.

M001 is Accepted: the exact approved `E:\Projects\3d-ulpin-proof-20260917` was removed after path/reparse-point checks. Its 230 old proof files were deleted; product and source-study directories remained present. See `tasks/M001_RESULT.md`. Do not interpret this as authorization to delete other data or new acceptance artifacts.

After each task, report the exact change/commit, tests actually run, pass/fail/blocked outcomes, limitations, review class and next task. Keep small useful evidence rather than recreating massive proof archives. Keep `backlog.json` as the progress authority after adoption; earlier workbook/JSON task boards remain historical. Original namespace mappings, 28 tests, 16 UI gates, 17 benchmarks and 25 input families remain in this pack.
