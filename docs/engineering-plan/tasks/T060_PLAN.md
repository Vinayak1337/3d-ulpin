# T060 — reconcile the two project chats and finish visual correctness

Starting revision: `da00fc1`, integrated by fast-forward from all newer origin branches on 20 September 2026. Work continues on `feat/visual-ml-completion`. The user authorized finishing remaining visual work and then genuine ML extraction; no new product restart is needed.

## Evidence and protected boundaries

Read both 3d Ulpin project chats: Build Context And Access Check and Push Files And Check Access. The later T059 report supplies missing captures but does not resolve the four defects identified in the former chat. Existing Studio fixture originals, persisted datasets, geometry, identities, revisions and repository snapshots remain unchanged. Private chat transcripts stay outside the repository. Retrieval covered all 21 turns across both chats; one older planning reply was capped at 20,000 characters by the retrieval tool, so its final tail was not recovered. Safari accessibility confirmed the later visual requirements; this is not a claim of byte-complete conversation retrieval.

## Ordered work

1. Fix map unit selection, URL-backed quick originals and shared header/search. Verify reload, Back, exact source selection, invalid links and dirty draft protection.
2. Make SVG exports derive from the selected source geometry rather than serializing the whole live map. Retain identities, source references, revision metadata and source credits; test scope, holes, mixed geometry and hostile labels.
3. Compare supplied map/register/workspace references with actual renders. Improve shared scene materials, foliage, lighting, architectural detail and inspection readability without changing analytical geometry or reshaping real Uttam Nagar.
4. Run current-build browser journeys, source checks, responsive/keyboard checks and fresh captures. Record limitations explicitly; physical-phone checks and user visual approval cannot be inferred from emulation.
5. Reconcile coverage with actual current tests before advancing to a separately planned ML implementation task.

## Verification and recovery

Use existing local services without resetting volumes or reimporting populated snapshots. Mutating journey checks use explicitly named synthetic verification workspaces. Keep local/reference and real datasets distinct. Run Studio, UI, spatial and export unit suites, typecheck, production build and actual browser verification. Compare reference captures side by side. Reversible code changes are isolated on this branch; no destructive data migrations belong to this task.

## ML follow-up boundary

A parallel read-only feasibility spike may establish free local inference and data provenance. Model integration follows this visual implementation pass. It must generate unreviewed source-linked candidates, preserve pixel/metric/frame distinctions, require evidence for elevations and record actual inference/evaluation rather than mocks. Document assistance is not building or floor segmentation.
