# T058 continuation — close workflow and visual gaps

Resume on the existing `feat/studio-reference-rebuild` branch at `6fe2d4f`.
The commit is present remotely and all three hosted checks completed successfully.
The prior result describes a working first pass, not an instruction to move on
while the user says the Studio work is unfinished. Continue this milestone before
ML or another data-schema task. Do not discard the copied Studio or original
Uttam Nagar sources, and do not regenerate the received source PDF under its ID.

## Ordered work

1. Reproduce omitted selection, deep-link, export, source-validation, local-draft
   and resource-error cases. Use atomic navigation and property-qualified previews.
   Check source must inspect data rather than emit an unconditional success toast.
   Invalid draft boundaries must not enter the review queue; saved floor/unit
   identity must be restored and source mismatches must not be silently overwritten.
2. Improve the actual shared 3D implementation: reusable facade/instance rendering,
   real exterior preview in the plan workspace, an exploded register with readable
   envelope and room detail, natural foliage, lighting, legible road labels and
   selected-parcel/road overlays. Display geometry must not rewrite source records.
3. Keep the original Studio layout as the base and compare the map, register,
   workspace, close view and mobile screens to the supplied image anchors at
   consistent dimensions. Fix observable defects instead of declaring visual
   completion from the unit-test count.
4. Rerun complete Studio browser flow plus new adversarial cases, shared-product
   regressions, source hashes, production build and representative visual captures.
   Preserve old evidence with dates and record new evidence separately.
5. Commit and push the incremental fixes on this branch, verify the remote SHA and
   actual hosted workflow conclusions, and leave the tested production preview and
   comparison gallery available. Do not merge to main without a request.

## Preservation and verification

The prepared v2 fixture and all received source assets remain byte-identical.
Reference-neighbourhood source rectangles are fixtures, not approximations of
real imported footprints. Decorative facades and exploded-floor offsets are
presentation only. The source-plan measurement engine remains shared and tested
against independent numeric expectations. One persistent Studio canvas is retained.
No recursive agent operations or credentials in evidence. The review status must
not imply user approval or pixel equality that was not actually established.
