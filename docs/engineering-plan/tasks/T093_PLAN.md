# T093 — legible 3D floor registry

Improve the shared registry floor stack, keeping exact supplied floor and unit polygons, holes, elevations and identities. Preserve the whole stack when a floor is selected; highlight its boundaries and mute other plates. Make model labels selectable and show actual elevation, with full IDs retained in the schedule. Consolidate duplicate view controls, compact floor rows, and show selected floor area/height/space counts and source access. Preserve mobile page scrolling. No section cut or floor isolation.

Do not label supplied geometry as ML extraction: ML review proposals remain separate until reviewed and linked. No canonical edits, source changes or data reseeding. Test courtyard/multipart preservation, selection styling and source immutability. Verify desktop/mobile selection, model navigation, source access and production build, then publish existing main demo.
