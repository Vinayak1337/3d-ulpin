# T067 — Declutter Studio map register and workspace

See [Studio UX plan](../STUDIO_UX_PLAN.md) for user intent, audit and preservation contract.

## Scope

Create consistent visual hierarchy and officer wording across maps, register, retained records and workspace.

## Implementation boundary

Keep one primary next action, quiet source/context labels, collapsed secondary findings/tools, useful selection details and adequate scene space. Preserve corrections, evidence, history, inquiries and scoped exports. Use actual source shapes; no fabricated heights. Check current captures against baseline and prefer familiar officer language.

Preserve previous T060/T061 changes in the dirty working tree. One task at a time; no subagent spawning from workers. Parent owns shared backlog and result acceptance.

## Verification

Desktop/tablet and keyboard/focus captures; floor/unit selection, findings, source/history/exports, advanced correction access; affected regression tests/build.

Do not claim user acceptance from automated tests. Record actual checks, changed files, remaining limits and next-task handoff.

## Corrected reference gate

T069 completed first per latest user steering. Use design/officer-studio-v3/DESIGN_BRIEF.md and REVIEW.md plus the actual revised screenshots as the presentation/interaction target. Do not copy errors from superseded original or exploratory draft images. Keep actual app capabilities honest while implementing these tasks sequentially.

## Default work queue and revised family

Apply the corrected 04-work-queue reference as default (optional preference unanswered; parent stated proposed assumption). Work queue / Map / Property Register are global destinations; Plan Workspace becomes contextual but /studio/workspaces and historical links continue resolving. Use actual persisted task state with honest wording, not fixture counts or cosmetic approval badges. T065 continuation state may be reused for linked preparation; queue also handles source-only work and running/failed jobs. No N+1 expensive model fingerprint computation across every list entry: return bounded server summaries with row continuation routes and validate fully on open. Empty/unknown cases must not be labelled review-ready. Keep saved block/dataset directory accessible from Map and work queue, and source-study/reference-quarter capabilities secondary.

Current ProductHeader has brand tagline + large search + section nav + dataset chooser, which compresses useful content at tablet width. Follow shared reference proportions, remove repeated slogan, retain keyboard search and contextual dataset selection. Search currently resolves saved record identities and authored reference properties only; label that scope honestly unless work search is actually added. Existing workspace-directory has LIMIT100 without pagination; do not claim complete All work until pagination/count is implemented or make the bound explicit.
