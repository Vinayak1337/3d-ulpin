# T086 — Show actual ML through source images and predictions

Replace the confusing selection/calibration/prism presentation with two explicit capabilities: building extraction and room segmentation. Show actual input raster → colored retained pixel mask → polygonized candidate overlay, using authentic retained public test results and a separately labelled current-dataset mode. Keep only short labels, model names, counts, provenance and recorded timing in the presentation. Do not imply ML inferred the city, floors, ownership, heights or identifiers. Preserve canonical data and all review editing.

Verify image/mask checksums against inference receipts before packaging, display masks by their retained label palette, and preserve model/source attribution. Build and inspect desktop/tablet layouts; exercise both capabilities, dataset switching, image visibility and return to review. Record results without claiming model accuracy.
