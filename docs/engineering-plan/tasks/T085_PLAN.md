# T085 — Visual-only hackathon presentation

Replace the text-led live explanation with a presentation canvas: actual source image, actual prediction outlines, selected candidate, calibration controls and computed prism. Use only short labels, names, counts and measurements. Keep source/review editing separate. Preserve data, model outputs and the candidate/fictional status. Verify stages, source switching, rotation and responsive layout in the browser.
