# T081 — Map movement, Shiv Vihar import and requirement coverage

User priority, 21 September 2026. Work solo. T080 durable receipt work remains planned.

1. Reproduce pointer navigation in the running shared viewport. Make ordinary dragging move the map with a discoverable rotate mode, scoped arrow-key movement, and clear gesture hints. Keep selection and all projection/presentation modes working.
2. Import the supplied Shiv Vihar MASTER package through the product. Verify source-derived counts, identities, missing-data disclosure and actual 3D view. Preserve original files and distinguish this draft from a saved record.
3. Audit statement 26011 against actual supported behavior and recorded limitations. Publish a clear gap matrix without claiming all extraction, ML, official issuance or arbitrary scale is complete.

Validation: production build; relevant shared geometry/import regression tests; native browser pointer drags, rotation, 2D, keyboard movement and Shiv Vihar import/search/register. Save actual browser captures and results. No source reset, fixture reseed, or inferred missing ownership/geometry.
