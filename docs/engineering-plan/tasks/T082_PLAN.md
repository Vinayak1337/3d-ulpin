# T082 — List both demonstration maps

User requests Lake View and Shiv Vihar in the dataset list. Add a shared bundled dataset catalog to the Map directory and header chooser. Default the directory to All datasets. Keep demos explicitly unrecorded and independent of the saved-area API. Give each a stable URL that loads its own original source ZIP through the shared normalizer; explicit URL selection must override the last active preview and survive refresh. Preserve saved-area navigation and existing custom imports.

Check production build, actual directory/search/filter, alternating dataset links, reload and header chooser. No database seeding or invented saved status. T080 durable intake remains separate.
