# T059 — finish the complete reference-defined frontend

Base: `0af92fd`, existing `feat/studio-reference-rebuild` branch. The user's latest
instruction covers **all** FE-A01–14 in the frontend audit, not another demonstration
tab or external-link milestone. Keep the authored Studio and all retained data.

## Product contract

One City Studio product shell serves dataset/register/workspace directories,
source-preserving saved maps, complete registers and operational workspaces.
Map selection keeps a same-page quick register with floor/unit and original
document inspection. Full register is a separate navigable dossier, not a label
over a placeholder. Reuse the existing officer domain components and APIs inside
the unified product. Do not use iframes, duplicate data stores or box substitutes
for irregular real geometry. Prepared fixture workflows stay visibly separate
from persisted records; an unreceived fixture has no invented investigation.

## Ordered implementation packages

1. **FE-A01–03: identity, navigation and real data.** Build a shared Studio header
   and operational route family, compatibility aliases, dataset/property search
   and directories. Load the existing normalized saved-scene descriptor into the
   shared map runtime. Preserve records/source URLs and world boundaries.
2. **FE-A04–08: complete inspection.** Quick register stays on the map; complete
   records reuse the actual overview, floor/unit, evidence, issues, retained
   revision and investigation components. Add original-page preview in the quick
   inspector, general sections for qualified geometry, independent parcel/utility
   selection and exact finding focus. History shows actual revisions, never a
   fabricated time slider over current geometry.
3. **FE-A09–12: complete processing and exports.** Bring source-aware measurement,
   original pages, known-distance/control calibration, source A/B compare,
   candidate build/placement/review/record and recovery into the same design.
   Property-aware create/link/import decisions preserve navigation context.
   Exports choose an actual scope/format and show included data/source counts.
4. **FE-A01/14: state and accessibility.** Route all leaving paths through shared
   unsaved-state protection, validate persisted map settings, retain keyboard
   combobox semantics and tablet/mobile panel strategies. Recover unsupported,
   empty, stale and failed states explicitly without inventing records.
5. **FE-A13/14: visual and functional acceptance.** Capture each reference family
   at 1672×941 plus tablet/mobile and actual 200% zoom, compare side by side, fix
   material layout/legibility gaps, and verify real saved data and the authored
   quarter. Run source/identity/regression/build checks and browser journeys,
   including controlled failure paths and actual backend transactions in a new
   explicitly labelled verification workspace where needed.

## Constraints and verification

The original source snapshot/PDF bundle is never regenerated to fit the UI.
No ML, general schema redesign, statutory claims or production-auth rollout.
Unknown heights/geometry are not filled from labels. Mutating validation uses
new explicitly named synthetic test records only; protect existing fingerprints.
Use local dependencies, existing APIs and the same map/runtime services.
Do not mark an item complete because it links to another untouched visual system.

Each FE item has a verification row in `docs/frontend-completion/coverage.json`.
Tests must match the displayed routes, tabs, sources and selected identities.
Record implemented, verified and any remaining unsupported capability separately.
Commit/push the actual implementation and leave a working production preview;
do not silently merge to main or claim user visual approval.
