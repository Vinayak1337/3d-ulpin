# T087 — Labelled ML sources and clean processing UI

Replaced Prepare evidence with ML extraction, removed redundant instructional paragraphs and duplicate navigation, and simplified the workspace to Sources / Results plus Present. The source directory groups the named aerial image under Buildings and the six B01 floor plans under Rooms, with per-file type badges, concise floor labels and basement-to-top ordering. Supporting reports are collapsed separately. Selection reveals only extraction task and PDF page. Selections survive tab/presentation navigation; starting a successful batch clears them.

Filename-based source suggestions remain editable and are not an image-classification claim. Supporting/unknown files require explicit task selection; page/batch bounds disable invalid requests. Earlier aerial-image runs using the room model are retained and labelled Source / task mismatch, with Choose correct task reopening the source selector using the building task without automatically starting a run. Result labels distinguish the source kind from the task actually executed.

Results retain original images, overlays, region selection, scores, notes and save actions, but show numbered regions rather than UUIDs. Calibration controls, saved proposals and technical receipts are collapsed disclosures. Existing review values and request payloads remain unchanged. All originals, model outputs and recorded geometry remain untouched; verification did not queue inference or save a review.

Validation: 3 focused source-classification/queue-gate tests passed, TypeScript and final production build passed. Browser verified building/room defaults, explicit task requirement for reports, invalid page blocking, selection persistence through Results and Present, actual retained room/building results, stored calibration values, mismatch correction, and the presentation/back path. Desktop and 768 px tablet inspected with no horizontal overflow; sticky action footer stays within the viewport. Browser errors absent. Screenshots in ../evidence/t087/. Normal viewport restored and only the existing port-3000 web server restarted.
