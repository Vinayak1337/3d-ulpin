# T078 — Reference scene reconstruction and map performance

User rejected the T077 map appearance and lag; register appearance accepted. Work solo. Reconstruct REF-01's visible neighbourhood as authored synthetic data using the existing source-only GeoJSON/CSV schema. The image pack contains no recoverable 3D source dataset. Preserve earlier originals; create a separate reference-anchor package and make it the showcase default. Keep source geometry distinct from display detail.

1. Rebuild the pictured block arrangement, park, road grid, low/mid-rise pale buildings, white parcel lines, focal red building and computed parcel/road conflicts. Bind search/floors/residents through existing normalization.
2. Reduce draw calls by batching static block facades across floors, with exact source floor meshes retained for register/sections. Stop repeated label-layout and full-canvas thumbnail work. Measure renderer statistics and interactive performance.
3. Actual product screenshot comparison and interaction checks, geometry/batching tests, production build. Keep only one product server running.

Acceptance is actual visual evidence and measured performance, not successful types alone. No claim of original survey data or perfect pixel identity from a raster image. Register remains functional and visually preserved.
