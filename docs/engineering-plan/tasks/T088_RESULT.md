# T088 — PDF controls, result arrows and complete demo import

Fixed extraction purpose for files already grouped as Buildings or Rooms; no task dropdown appears on those cards. Supporting/unclassified sources still require explicit classification. Actual retained PDFs are parsed for page count with the already-installed PDF.js server parser, cached by source hash with a bounded process cache; original size/hash is verified before parsing. Corrupt/locked/unreadable documents show unavailable counts rather than invented totals. Sources now show page counts and a bounded minus/number/plus selector. The queue independently rejects out-of-range PDF pages before creating a job. Result navigation adds previous/next arrows, a position counter and disabled end boundaries, retaining the direct source chooser.

Read-only audit: both saved building-model runs returned 10 candidate regions, with no polygon-size/complexity/capacity omissions. Only about five list rows were visible without scrolling. Their 220×230 synthetic raster is unlike the real aerial imagery expected by the pretrained model; missed/merged predictions cannot be interpreted as a complete building inventory. The map's 49 buildings come from supplied vector geometry and schedules. ML misses are not automatically matched to canonical buildings as discrepancy flags.

Existing map conflict UI was verified without changing the topology engine: B01 is red and carries a conflict label, its inspector gives two review findings, and the findings dialog measures 21.6 m² outside P01 and 21.6 m² overlap with R04. These refer to the same authored strip. Building-to-building checks account for compatible known vertical intervals and exclude shared edges. Checks are bounded to qualified polygon/metre-frame comparisons and 100-building previews; they are not all possible validation or legal acceptance. No automatic geometry repair or ownership resolution is implemented or claimed.

Created data-source/lake-view-complete.zip, byte-identical to the existing complete package, plus all 52 unpacked archive members, checksums, computed import-verification JSON and README import/demo instructions. Source manifests and every declared member hash/size verified. Actual browser upload from this folder produced 49 buildings, 184 floors, 189 spaces and 50 source files, opened the full map, and Save dataset reused the existing Lake View ID at revision 1. Original records/history were retained; no inference was queued or review written by verification. Updated the old source showcase guide to reflect saved-dataset support.

Validation: 6 focused tests passed (source routing, actual multi-page/blank-page PDF count, malformed PDF, count-aware selection bounds); TypeScript and production build passed. A real backend request for page 2 of a one-page retained PDF was rejected with no new jobs. Browser checked 1-page and 6-page displays, disabled page boundaries, increment/decrement, fixed tasks, result next/previous/end boundary, retained result counts, map flags, full-package upload/render/save and same-ID readback. Browser error log empty. Evidence: ../evidence/t088/. One port-3000 web server remains; no private services or source volumes were reset.
