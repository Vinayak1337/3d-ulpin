# T075 result

20 September 2026. Shared import contract gate implemented; visual work follows in T076.

The manifest-backed ZIP/JSON receipt retains original bytes and hashes, verifies every packaged source, rejects unsafe/undeclared/altered content and counts actual decompressed output. The version-pinned adapter builds `ulpin-spatial/2` and explicit disposable renderer projections. Dense and independent fixtures compile through the existing tile compiler. Presentation choices are whitelisted and bound to the canonical snapshot digest; rights and findings cannot enter that sidecar.

23 focused package/adapter/presentation tests pass, including invalid archives, unchanged source bytes, separate IDs, multipart/courtyard/rotated geometry, +100 m base, missing geometry, historical revisions and malformed holes. Web typecheck passes. The independent review found two archive integrity defects; both were corrected before this result.

The local core profile does not yet model XYZ alignment analysis. Original utility XYZ coordinates remain preserved, with unavailable analytical representations and explicit diagnostics. No guessed heights, implicit frame mixing or undeclared reprojection. The detailed display profile remains bounded (100 buildings, 1,000 geometry records, 2 km extent); larger streamed profiles are separate.

No live data writes or seeds. The shared browser build is reproducible with `node scripts/reference/build-shared.mjs`. Existing standalone UI wiring and shared viewport integration are the next task, so this result does not claim reference visual acceptance.
