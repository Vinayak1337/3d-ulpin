# T086 — Actual ML images and outputs

Replaced the selection/calibration/prism presentation with two capabilities: Building extraction (RF-DETR) and Room segmentation (CubiCasa5K). Each shows a real retained model-input raster, the actual integer-label prediction mask colored by its retained palette, and the original polygonized candidate overlay. Controls switch capabilities, public test/current dataset evidence, and source-image visibility. Only short labels, model names, recorded timing, counts and a class legend are visible; provenance and filtering details sit in a disclosure. Back/Present preserve the existing review workflow.

Public examples are the retained T061 OAM tile 1200 and CubiCasa plan 1191. Both raster/mask byte hashes, every component, model identity and complete receipt were verified against the original inference evidence. No inference was fabricated or rerun for this presentation; recorded timings are labelled. Public source bytes and attribution were already present; the two unchanged raw masks and their receipts are now packaged alongside them. The named current-dataset mode reads existing saved runs and artifacts. No source attachment, canonical geometry, review or ML output was modified.

Validation: TypeScript and final production build passed; browser exercised both model tabs, both Lake View task results, public/dataset switching, outline-only mode, provenance disclosure, Back to review and Present. Images and masks load without browser errors. Presentation has zero paragraph/blockquote nodes. Desktop composition and 768 px tablet layout inspected, with document width equal to viewport width. Tablet capture is scaled/letterboxed by the in-app browser; desktop captures are normal. Browser viewport restored. Removed the nonresponsive native full-screen action; the presentation occupies the page. Evidence and artifact verification are in ../evidence/t086/.

Limits remain explicit: these are candidate pixel/region predictions, including errors and omissions, not survey-qualified building/floor reconstruction or ownership. Floor-plan counts include class fragments, not a count of legal rooms or units. Retained masks preserve regions omitted by the existing polygonization limits. The separate source/calibration/3D review functionality and historical /explain material remain available.
