import { Wordmark } from '@ulpin/design-system';

export const Surfaces = () => (
  <div className="ul-stack">
    <Wordmark surface="Studio" />
    <Wordmark surface="Admin" />
    <Wordmark surface="Property Card" size={15} />
  </div>
);
